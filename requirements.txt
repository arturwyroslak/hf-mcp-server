# MCP Python SDK
mcp>=1.6.0

# HuggingFace Hub
huggingface_hub>=0.26.0

# ASGI server
uvicorn[standard]>=0.29.0

# Fast HTTP client with HTTP/2 support
httpx[http2]>=0.27.0

# Fast JSON serialization (2-10x faster than stdlib json)
orjson>=3.10.0

# Gzip compression middleware
starlette>=0.37.0
