# MCP Python SDK (provides FastMCP + streamable_http_app)
mcp>=1.6.0

# HuggingFace Hub
huggingface_hub>=0.26.0

# ASGI server — required to serve the Starlette app returned by streamable_http_app()
uvicorn[standard]>=0.29.0

# HTTP client (used internally by huggingface_hub)
requests>=2.28.0
